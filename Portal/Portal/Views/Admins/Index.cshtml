@model IEnumerable<Portal.Models.Admin>


<!--StudentIDViewModel-->

@{
    ViewData["Title"] = "Student's List";
}


<div class="text-center">
    <h1 class="display-3">Student's List</h1>
</div>

<div>
    <a class="btn btn-dark float-right" asp-action="Create">Add New Student</a>
</div>
<!--
<form asp-controller="Admins" asp-action="Index" method="get">
    <p class="text-light"> wala na? tapos na? bukas na ulit???
        Student Number: <input type="text" name="SearchString" asp-for="AdminStudentID" asp-items="Model.StudentID" />
        <input type="submit" value="Search" />
    </p>
</form>
    -->
<br />
<br />


<table class="table table-dark table-striped">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StudentID)
            </th>
            <th>

                @Html.DisplayNameFor(model => model.Last_Name)

            </th>
            <th>
                @Html.DisplayNameFor(model => model.First_Name)
            </th>
            <!--<th>
                Html.DisplayNameFor(model => model.Birthdate)
            </th>
            <th>
               Html.DisplayNameFor(model => model.Age)
            </th>
            <th>
                Html.DisplayNameFor(model => model.Address)
            </th>-->
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.StudentID)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Last_Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.First_Name)
                </td>
                <!-- <td>
                     Html.DisplayFor(modelItem => item.Birthdate)
                 </td>
                 <td>
                     Html.DisplayFor(modelItem => item.Age)
                 </td>
                 <td>
                     Html.DisplayFor(modelItem => item.Address)
                 </td>
                 -->
                <td>
                    <div class="btn-group btn-group-sm" role="group" aria-label="Basic example">
                        <a class="btn btn-small btn-success" asp-action="Edit" asp-route-id="@item.ID">Edit</a>
                        <a class="btn btn-small btn-secondary" asp-action="Details" asp-route-id="@item.ID">Details</a>
                        <a class="btn btn-small btn-danger" asp-action="Delete" asp-route-id="@item.ID">Delete</a>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>
